language: python
python:
  - "3.7"

branch: gh-pages

install:
  - pip install -r requirements.txt
script:
  - python3.7 corona_scraper.py

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  on:
    branch: gh-pages